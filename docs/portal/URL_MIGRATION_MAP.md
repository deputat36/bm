# Карта миграции URL портала novostroyki-borisoglebsk.ru

Дата обновления: 2026-07-13

Репозиторий: `deputat36/bm`.

## 1. Текущее состояние

Корень сайта уже переведён на независимый городской портал:

```text
/ = Новостройки Борисоглебска
```

Рабочая структура первой очереди:

```text
/
/catalog/
/catalog/prostornaya-4a/
/catalog/aerodromnaya-18g/
/catalog/sennaya-76/
/developers/
/ipoteka/
/guides/
/news/
/contacts/
/privacy/
/spasibo/
```

Новые внутренние разделы пока сохраняют `noindex,follow`, если их данные, формы или материалы ещё не прошли финальную проверку.

## 2. Главное правило миграции

```text
переходная страница → noindex,follow → проверенный целевой URL → контролируемый серверный редирект
```

Нельзя удалять старый URL или направлять его на неподготовленную страницу.

## 3. Машиночитаемый реестр

Первая очередь переходных маршрутов хранится в:

```text
data/migration/legacy-routes.json
```

Реестр проверяет команда:

```bash
npm run validate:legacy
```

В реестре фиксируются:

```text
source_url
source_file
target_url
target_file
target_href
status
redirect_phase
redirect_ready
blocking_reason
```

## 4. Первая очередь legacy-URL

| Старый URL | Целевой URL | Текущий статус | Действие сейчас |
|---|---|---|---|
| `/portal-preview/` | `/` | переходная архивная страница | `noindex,follow`, ссылка на рабочую главную |
| `/novostroyki/` | `/catalog/` | переходная страница | `noindex,follow`, ссылка на новый каталог |
| `/zhk/` | `/catalog/` | переходная страница | `noindex,follow`, объединение ЖК и новых домов в каталоге |
| `/zastroyschiki/` | `/developers/` | переходная страница | `noindex,follow`, ссылка на нейтральный справочный раздел |
| `/spravochnik/` | `/guides/` | переходная страница | `noindex,follow`, ссылка на новый справочник |
| `/novosti/` | `/news/` | переходная страница | `noindex,follow`, ссылка на новости портала |

На этих страницах удалены старый домен, брендинг одного агентства или застройщика, устаревшие формы и ссылки на старую навигацию.

## 5. Почему редиректы пока не включены

Серверные редиректы первой очереди вводятся только после выполнения всех условий:

1. Целевой URL существует и открывается без ошибок.
2. На целевой странице нет неподтверждённых фактов.
3. Формы и системные ID заявок проверены.
4. Юридические формулировки прошли валидацию.
5. Принято решение об индексации целевого URL.
6. Определён способ настройки редиректов на используемом хостинге.
7. Подготовлен план отката.

До этого используются переходные HTML-страницы без автоматического перенаправления.

## 6. Вторая очередь объектных URL

Следующая очередь относится преимущественно к старому контуру Просторной 4А / ЖК «Теллерманов сад»:

| Старый URL | Предварительный целевой URL |
|---|---|
| `/prostornaya-4a/` | `/catalog/prostornaya-4a/` |
| `/zhk/tellermanov-sad/` | `/catalog/prostornaya-4a/` |
| `/o-zhk/` | `/catalog/prostornaya-4a/` |
| `/kvartiry/` | `/catalog/prostornaya-4a/` |
| `/planirovki/` | `/catalog/prostornaya-4a/` |
| `/ceny/` | `/catalog/prostornaya-4a/` |
| `/dokumenty/` | `/catalog/prostornaya-4a/` |
| `/galereya/` | `/catalog/prostornaya-4a/` |
| `/infrastruktura/` | `/catalog/prostornaya-4a/` |
| `/hod-stroitelstva/` | `/catalog/prostornaya-4a/` |
| `/zastroyschik/` | будущая подтверждённая карточка застройщика |
| `/proektnaya-deklaratsiya-36-001139/` | раздел документов объекта |

Эти URL нельзя массово перенаправлять до повторной проверки содержимого и полезности каждой страницы. Часть страниц может быть сохранена как информационные материалы, если у неё есть самостоятельная ценность.

## 7. Служебные URL

| URL | Решение |
|---|---|
| `/zayavka/` | оставить закрытой от индексации до решения о назначении |
| `/spasibo/` | сохранить как служебную страницу после отправки формы |
| `/privacy/` | сохранить |
| `/personal-data-consent/` | сохранить |
| `/legal/` | проверить и сохранить как правовую информацию |
| `/sources/` | проверить и сохранить как страницу источников |

## 8. Правила sitemap и robots

В `sitemap.xml` нельзя включать:

```text
переходные legacy-URL
страницы со статусом requires_check
страницы с is_public_ready=false
служебные страницы заявок и благодарности
```

`robots.txt` должен указывать только актуальный sitemap нового домена.

## 9. Требования к будущим редиректам

Предпочтительный вариант — постоянный серверный редирект `301` или эквивалентное правило платформы.

Не использовать как финальное решение:

```text
meta refresh
JavaScript redirect
цепочки из нескольких редиректов
редирект всех старых URL только на главную
```

Каждый старый URL должен вести на наиболее близкую по смыслу страницу.

## 10. Контроль перед релизом редиректа

Для каждого маршрута проверить:

1. HTTP-статус старого URL.
2. Один переход без цепочки.
3. Финальный URL и canonical.
4. Отсутствие старого URL в sitemap.
5. Работу мобильной версии.
6. Сохранение UTM-меток, если редирект их поддерживает.
7. Отсутствие циклического перенаправления.
8. Возможность быстро отменить правило.

## 11. Следующий этап

1. Провести аудит второй очереди объектных страниц.
2. Добавить проверенные маршруты в `data/migration/legacy-routes.json`.
3. Разделить страницы на `redirect`, `retain` и `retire`.
4. Определить фактическую платформу размещения и формат серверных правил.
5. После контролируемого теста выпустить первую очередь редиректов.
