# Журнал работ — 12 июля 2026

## Выполнено

### Реестр приоритетных объектов

Создан:

```text
data/research/priority-projects.json
```

В нём зафиксированы три объекта с отдельной лидогенерацией:

```text
Просторная 4А
Аэродромная 18Г
Сенная 76
```

Для каждого объекта указаны:

```text
системный ID
публичный slug
адрес
роль priority_lead
тип заявки project_consultation
form_id
статус проверки
готовность к индексации
страница
файл данных
следующее действие
```

### Роли объектов в каталоге

В `data/projects/index.json` добавлены поля:

```text
commercial_role
lead_type
lead_form_id
```

Три приоритетных объекта имеют:

```text
commercial_role: priority_lead
```

Остальные новостройки в дальнейшем должны добавляться как:

```text
commercial_role: reference_catalog
```

### Карточки Аэродромной 18Г и Сенной 76

В JSON-профили добавлены:

```text
commercial_role
lead_type
lead_form_id
can_collect_preliminary_leads
```

Сбор предварительных заявок разрешён, но страницы остаются `noindex,follow`, не входят в sitemap и не должны публиковать неподтверждённые цены, планировки, сроки или застройщика.

### Схема справочного каталога

Создан документ:

```text
docs/portal/REFERENCE_CATALOG_SCHEMA.md
```

Он определяет минимальную структуру краткой карточки для остальных новостроек Борисоглебска.

### Автоматические проверки

Добавлены команды:

```text
npm run validate:priority
npm run audit:branding
```

`validate:priority` проверяет согласованность:

```text
реестра приоритетных объектов
индекса проектов
файлов данных
страниц
form_id
lead_type
robots
```

`audit:branding` проверяет основные страницы портала на возврат старого домена, логотипов BM Group, класса старого бренда и ссылок на архивную главную.

## Ограничение проверки

Локальный запуск npm-команд в текущем рабочем окружении не выполнен: контейнер не смог разрешить домен github.com для клонирования репозитория.

Изменённые файлы повторно проверены через GitHub-коннектор. Формы трёх приоритетных карточек содержат ожидаемые `lead_type` и `form_id`.

## Следующий этап

1. Добавить валидатор справочного реестра.
2. Собрать подтверждённые данные по другим новым домам Борисоглебска.
3. Вывести краткие справочные карточки в `/catalog/`.
4. Продолжить аудит старых публичных страниц одного ЖК.
5. Подготовить контролируемый тест доставки одной служебной заявки.
