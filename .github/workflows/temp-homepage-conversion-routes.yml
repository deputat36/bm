name: Temporary homepage conversion routes patch

on:
  push:
    branches:
      - agent/homepage-conversion-routes

permissions:
  contents: write

jobs:
  patch-homepage:
    if: ${{ !contains(github.event.head_commit.message, '[homepage-routes-connected]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/homepage-conversion-routes
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply focused patch
        run: node tools/apply-homepage-conversion-routes.mjs

      - name: Commit homepage
        run: |
          if git diff --quiet -- index.html; then
            echo "No homepage changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "[homepage-routes-connected] Разделить пользовательские маршруты главной"
          git push origin HEAD:agent/homepage-conversion-routes