name: Temporary project verification summary patch

on:
  push:
    branches:
      - agent/project-verification-summary

permissions:
  contents: write

jobs:
  patch-pages:
    if: ${{ !contains(github.event.head_commit.message, '[verification-summary-connected]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: agent/project-verification-summary
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply focused patch
        run: node tools/apply-project-verification-summary.mjs

      - name: Commit patched pages
        run: |
          if git diff --quiet -- catalog/prostornaya-4a/index.html catalog/aerodromnaya-18g/index.html catalog/sennaya-76/index.html; then
            echo "No page changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog/prostornaya-4a/index.html catalog/aerodromnaya-18g/index.html catalog/sennaya-76/index.html
          git commit -m "[verification-summary-connected] Подключить агрегаты проверки объектов"
          git push origin HEAD:agent/project-verification-summary